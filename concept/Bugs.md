# CityDeveloper — Баги

_Баги — отдельный поток от diff. Они не считаются при подсчёте сходимости diff
(фича может быть ✅ и при этом иметь баг)._

## Активные

| ID | Приоритет | Статус | Связан с F-NN | Описание | Воспроизведение | Ожидаемое |
|----|-----------|--------|---------------|----------|-----------------|-----------|
| BUG-002 | P2 | 🔴 Открыт | F-11 | Окно журнала открывается в случайной позиции | Кликнуть `list.bullet` → floating-окно появляется по умолчанию macOS WindowController (центр или последняя позиция) | Открывается прижатым к правой стороне экрана, по вертикальному центру |
| BUG-004 | P2 | 🔴 Открыт | F-18 | Невозможно подключить Apple Notes как источник — только folder с .md файлами | Settings → Notes watcher → можно выбрать только папку. Свои заметки из встроенного Notes.app подключить нельзя | Добавить адаптер для Apple Notes (через AppleScript / ScriptingBridge / NoteStore.sqlite). Backlog → новая F-NN, не быстрый фикс. |
| BUG-005 | P1 | 🔴 Открыт | F-14, F-20 | Нет UI для сброса состояния и replay из git c указанной даты | Сейчас single-shot CatchUpScheduler сканит репозитории с `lastCheckTs`. Если нужно «затереть всё и начать с 2025-01-01» — приходится вручную удалять `events.jsonl`/`state.json`/`catchup-state.json` | Settings → Reset section: кнопка «Reset city», DatePicker «Replay since», подтверждение «Сотрётся текущий город. Все коммиты с указанной даты будут импортированы заново.». |
| BUG-023 | P2 | 🔴 Открыт | F-13, F-16 | Визуальное однообразие производственных юнитов: `farm` / `farmHouse` / `market` / `warehouse` / `workshop` рендерятся через единый `UnitSprites.makeCategoricalBuilding` → визуально это «куб с пирамидальной крышей разных оттенков». Категории неотличимы пользователю. На скриншоте 2026-05-25 ~30% юнитов выглядят как clones одного силуэта. | Визуально на любом stage 3+ квартале с production-юнитами (например, current state.json: 7 farm + 7 farmHouse + 7 warehouse) — все одинаковые «cube + roof» формы. | Уникальные силуэты по категориям: farm = открытое поле с грядками; market = навес/тенты на стойках; warehouse = длинный низкий ангар без острой крыши; workshop = постройка с трубой/дымом; mill = высокая башня с лопастями. Минимум — different roof shapes (flat/gabled/domed/peaked) + different ground footprint patterns (grid/scattered/walled). См. также backlog idea «Архитектурный редизайн юнитов — уйти от пластиковых кубиков». |
| BUG-027 | P2 | 🔴 Открыт | F-06 | TASK-058 AC4: нет property-теста сортировки руин при 2+ кандидатах. Логика в `CityEngine.pickRuinForNewProject` (CityEngine.swift:980-990) сортирует `lastActivityAt asc → unitIds.count desc → id asc` — реализована корректно, но тесты покрывают только сценарий с одним decay-4 кандидатом. | Запустить тесты — `test_NewProject_OccupiesRuin_WhenDecay4Exists` использует один decay-4 проект. Случай «2+ decay-4 кандидата, должен быть выбран старейший / самый большой / лексикографически меньший» не покрыт. | Добавить тест `test_AC4_RuinPriority_TwoCandidates` (и при необходимости _ThreeCandidates) с проверкой всех трёх правил сортировки в `Tests/CityDeveloperTests/RuinReoccupationPropertyTests.swift`. Логика кода не меняется. _Источник: TASK-058 verify (commit 96cfd12), 2026-05-26._ |
| BUG-028 | P2 | 🔴 Открыт | F-06 | TASK-059 doc-комментарий вводит в заблуждение: «max 24 позиции» — реально 16 уникальных (3 кольца × 8 слотов; `ring = min(idx/8+1, 3)` зажимается на 3 при `idx ≥ 16`, последние 8 итераций цикла дают те же позиции что 8 предыдущих). Defensive путь работает корректно, но при анализе кода путаница. Дополнительно — нет автотеста на «все 24 idx блокированы → ErrorsLog warning + defensive return» (AC6 из TASK-059 покрыт только реализацией). | Прочитать `UnitPlanner.swift:472-505` — doc-коммент говорит «24 позиции», реально уникальных 16. Эмпирически: при i=0, j=16 → ring=3, slot=0 = та же pos что j=8 (ring=3, slot=0). | (1) Уточнить doc-коммент: «max 16 уникальных (3 кольца × 8); цикл обходит 24 idx для сохранения детерминированного порядка от i». (2) Опционально — упростить цикл до `for j in 0..<16` (если ring всё равно зажат). (3) Добавить unit-тест `test_AllRingPositionsBlocked_DefensiveReturn` с искусственно заполненным `otherProjectCells` всеми 16 позициями + проверка ErrorsLog. _Источник: TASK-059 code-review (commit ed924c3), 2026-05-26._ |

## Закрытые

| BUG-026 (partial) | 2026-05-26 (commit 396cbd9) | F-06: город разрастался линией вдоль одной магистрали, не было ответвлений и spatial-разнообразия. | **TASK-063 закрывает branching стратегию (MVP):** `DistrictPlanner.branchingThreshold=3` — первые 3 origin'а вдоль магистрали, с 4-го (`idx >= 3`) — branching формула `layer/sub/perpOffset=minDistrictRadius*layer` (origin отходит перпендикулярно от магистрали с шагом 8 поочерёдно вверх/вниз). Новый `RoadNetwork.extendBranchToOrigin(...)` строит road-сегмент `v=1..perpOffset-1` от ближайшей mag-клетки до периметра нового квартала (water + cross-project hard-block; atomic insert через 2-pass проверку). `CityEngine.applyTaskCompleted` legacy-ветка вызывает `extendBranchToOrigin` перед `planDistrict`; `syncRoadNetworkPlans` patch для replay-determinism (AC7). Property-тесты `DistrictBranchingPropertyTests` × 3 (perpendicular offset, road branch path, deterministic replay). 182/182 full suite. **Что осталось (followups в Backlog):** Settings UI переключатель стратегий (spiral/grid/template/auto-by-seed), district-placement templates по family (roman-grid / greek-hippodamian / egyptian-spiral), мосты через воду для branching offset, bounds upper-check (для 128+ проектов), dedicated negative-tests, biomeReader-presence invariant test. Lead: opus (P1+multi-module), Run: sonnet, Code-review: opus (approved, 3 non-blocking → followups). |
| BUG-009 | 2026-05-26 (commit ee46658) | F-06/F-15: кварталы строились прямо на воде — DistrictPlanner игнорировал BiomeMapReader. | **Корневой fix — TASK-030c + TASK-056** (water-skip + cross-project skip в `DistrictPlanner.allocateNextOrigin` / `allocateAlongMagistrale`; порядок water → cross-project → preferred, см. `DistrictPlanner.swift:40-118, 180-200`). **TASK-062 (этот коммит):** добавлены defensive ErrorsLog.warning при исчерпании спирали/магистрали в обоих методах + регресс-щит `DistrictPlannerBiomeAwareTests` (3 новых: `test_AllWaterMapTriggersErrorsLogWarning`, `test_AllWaterMagistraleTriggersErrorsLogWarning`, `test_MeadowMapDoesNotTriggerWarning`). Test seam в `ErrorsLog.writer` для перехвата записей в тестах. 179/179 full suite. Lead: opus (P1), Run: sonnet, Code-review: opus (approved). |
| BUG-003 + BUG-007 | 2026-05-26 (commit 065f9a8) | F-14: Settings окно слишком маленькое (BUG-003) + секции уезжают за окно при 2+ репо (BUG-007). | TASK-060: один фикс закрывает оба. `SettingsWindowController.swift`: `styleMask = [.titled, .closable, .resizable]` (добавлен `.resizable`); content size 720×600 → 800×720; minSize 640×480 → 720×500. `SettingsView.swift:146`: `.frame(minWidth: 720, minHeight: 500)`. Outer `ScrollView(.vertical)` уже существовал на SettingsView:47 — корень проблемы был styleMask без `.resizable` + content 720×600 < суммарной высоты секций ~900pt. Минимальное изменение — window-level + одна строка `.frame`. Тесты не сломаны (176/176). Manual GUI проверка AC1-AC5 требует запуска приложения. Lead: opus (P1), Run: haiku (junior/S, 4 точечные правки), Code-review: opus (approved). |
| BUG-025 | 2026-05-26 (commit ed924c3) | F-06/F-15: `UnitPlanner.legacyRingPosition` не проверял `otherProjectCells` — в pre-mainRoad сценарии (первый юнит проекта до появления road) возможна была постановка поверх клеток чужого проекта. Edge case не покрытый TASK-056 фиксом BUG-022. | TASK-059: `legacyRingPosition` расширен опциональными параметрами `builtCells/otherProjectCells: Set<GridPoint> = []` (back-compat). Внутри — цикл по 24 idx (`for j in 0..<24`) с проверкой через `footprintBlocked(...)`: возвращает первую незаблокированную позицию в детерминированном порядке. Если все 24 idx блокированы — `ErrorsLog.write` + defensive return i-й позиции по исходной формуле (без crash). Прокинуты в оба call site `nextPosition` (`roadCells.isEmpty` и `nearby.isEmpty`). Property-тесты `UnitPlannerLegacyRingOverlapTests` × 2 (NoOverlap + DeterministicReplay), CityEngine-level через `roadNetwork=nil`. Регресс `DistrictNoOverlapPropertyTests` × 2 + UnitPlanner suite — pass. Full suite 176/176. Followup: «24 позиции» в doc-комменте — реально 16 уникальных (3 кольца × 8 слотов; `ring = min(idx/8+1, 3)` зажимается на 3 при idx≥16), defensive путь работает корректно, только комментарий нуждается в уточнении. |
| BUG-024 | 2026-05-26 (commit 96cfd12) | F-06/F-15: после decay-4 (ruins) клетки/claim проекта не освобождались — DistrictPlanner считал их занятыми, новый проект обходил «мёртвую зону» вместо reoccupation. | TASK-058: `CityEngine.claimedCellsByProjects(in:includeDecayedRuins:)` получил опциональный параметр `includeDecayedRuins: Bool = true` (back-compat default). На единственном cross-project-skip call site в applyTaskCompleted fallback-ветке (CityEngine.swift:326) передаётся `includeDecayedRuins: false` — клетки decay-4 проектов и orphan units исключаются из карты, новый проект может встать рядом с руинами или поверх. UnitPlanner in-district placement call site (otherSet строится напрямую через state.units) намеренно НЕ трогаем — там hard-block decay-4 нужен до ruin-ветки. Property-тесты `RuinReoccupationPropertyTests` × 5 (AC1/AC2/AC3/AC5/AC6 покрыты). Регресс `DistrictNoOverlapPropertyTests` × 2 PASS. Full suite 206/206. AC4 (приоритет «старейшая/большая руина» при 2+ кандидатах) — minor test gap, логика в `pickRuinForNewProject` (CityEngine.swift:980-990) реализована корректно. Совместимость с TASK-059 сохранена (его otherProjectCells собирается отдельно из state.units). |
| BUG-008 | 2026-05-25 (commit d13f490) | F-15: на свежем seed доминирует один биом (>55% карты, чаще desert/sand) — нет визуального разнообразия 4+ цветов. Старый текст «80%+ grass» устарел: после обновлённой палитры 2026-05-23 фактическая доминанта — другой биом, эффект тот же. | TASK-057: `BiomeClassifier.classify(world:strict:)` возвращает `ClassificationOutcome` с метрикой `nonWaterAboveThreshold` (число неводных биомов из {meadow, desert, forest, mountain, stone} с долей ≥5%) и `dominantShare`. `WorldMapProvider.generateWithRetry` пробует до 5 seed'ов (`requested, requested+1, …, requested+4`), останавливается на первой `balanced`-карте. Фактически использованный seed сохраняется в `worldmap.json` и `WorldSeedStore`. AppSettings получил `requestedMapSeed` (эфемерное), UI показывает «Seed: requested N → actual M» при retry. Property-тест `BiomeDistributionPropertyTests` × 2: 10/10 захардкоженных seeds после retry проходят инвариант (на baseline даже без retry 10/10 — текущая классификация типично сбалансирована). `minDiversity` снижен с 6 до 4. Косвенно закрывает BUG-006 (см. ниже). |
| BUG-006 | 2026-05-25 (commit d13f490) | F-15: цвета биомов «сломаны» — на карте только 2-3 цвета. | Закрыт косвенно через TASK-057: палитра не менялась (актуальная с 2026-05-23, commit 81b829a); причина «только 2-3 цвета» — перекошенное распределение биомов (BUG-008). После balanced-distribution + retry на bad-seed карта стабильно содержит ≥4 неводных биомов ≥5% каждый → 4+ цвета палитры видны автоматически. Retest не требуется. |
| BUG-022 | 2026-05-25 (3257943) | Кварталы разных проектов перекрываются в state.json — фундаментальная регрессия F-06 (на реальной сцене 4 проекта × ~20 задач: 7 overlap cells, 15 юнитов в конфликтных позициях) | TASK-056: `DistrictPlanner.allocateNextOrigin` + `allocateAlongMagistrale` получили параметры `otherProjectsClaims: Set<GridPoint>` + `minDistrictRadius=8`. Перед поиском origin собирается `Set<GridPoint>` всех клеток чужих проектов через computed helper `CityEngine.claimedCellsByProjects(in:)` — на лету из `state.units`, без миграции CityState формата. Спираль пропускает origin'ы в Чебышёвской окрестности `minDistrictRadius` от чужих клеток. `UnitPlanner.nextPosition` + `footprintBlocked` получили `otherProjectCells: Set<GridPoint>` (hard-block в template path и legacy path). Property-тест `DistrictNoOverlapPropertyTests` × 2 (ZeroOverlap + DeterministicReplay): 5 проектов × 30 задач → 0 overlap, replay детерминирован. Существующий state.json пользователя — рекомендуется reset (детерминизм replay сохраняется только при пустом старте). Known limitation: decay-4 руинные проекты не освобождают claim → новый проект обходит «мёртвую зону» через cross-skip (отдельная задача). |
| BUG-001 | 2026-05-25 (f55b297) | Две информационные плашки при клике на дом: SpriteKit `InspectorPanel` (world-coords) + SwiftUI `InspectorOverlayCard` (screen-coords) одновременно | TASK-055: финальный cleanup. SpriteKit-popup вызов был закомментирован в GameScene ещё в частичном фиксе; TASK-055 удалил deprecated `Game/InspectorPanel.swift` целиком (через `git rm`) + поле `inspector: SKNode?` (GameScene.swift:34) + self-click защиту `if n === inspector` (GameScene.swift:978) + закомментированные строки в `showInspector`/`hideInspector` + ссылку в doc-comment `UI/InspectorOverlayCard.swift:4`. SwiftUI overlay остался единственным каналом отображения через `bridge?.selectedUnitInfo`. `grep -r InspectorPanel Sources/` = 0 строк. |
| BUG-021 | 2026-05-25 (c0c7268) | На stage5/meadow ~37% юнитов large=true (56/150 в `test_EdgeCase_LargeUnitsAppearRarely`) — теряется визуальное разнообразие квартала | TASK-054: `UnitPlanner.weightedPick` получил `largeRarityFactor = 0.1` + helper `isLarge(kind)` по флагу `kind.large` (НЕ size — 16 kinds 2×2+ имеют large=false: farmhouse/market/mill — для них множитель не применяется). Эволюционный канал `EvolutionGraph` не задействован. Тест baseline 60→16 (фактическое ~13/150 при семействе 17 large из 51 kinds; AC4 spec=10 пересмотрен под реальный каталог). |
| BUG-020 | 2026-05-25 (e63c8bc) | `testRiversHaveReasonableWidth` падал на любом seed: рек нет ни при каком — `BiomeClassifier.carveRivers` был удалён из pipeline ещё в 31acaad («Реки отключены — плохо рендерились, пересекали город и дороги»). Won't-fix как баг; TASK-053 = cleanup: удалены мёртвые `carveRivers`/`carveOnePath`/`riverSourceCount`/`maxRiverHalfWidth` в `BiomeClassifier.swift` + удалён `testRiversHaveReasonableWidth`. Enum `BiomeKind.river` сохранён (используется в CityState/terrain matching, TileTextureFactory). Возврат фичи требует сначала решить рендер + road-conflict — отдельный продуктовый запрос. |
| BUG-019 | 2026-05-24 (f0e727a) | Дальние юниты/road-клетки перекрывали ближние — y-sort не учитывал layer-z для road | `GameScene.drawUnit:380` — введён `farSum = x + y + w + h − 2` (дальний угол footprint'а) + `layerOffset = -0.5` для `kind == .road`. Layer-z иерархия: road < buildings < citizens (`+0.5` в `CitizenManager`). Property-щит — 5 кейсов в `ZSortInvariantsTests` (1×1+1×1, 1×1+2×2 anchor-fix регресс TASK-044, 2×2+2×2, road-vs-building same farSum, far-road-vs-near-building cross-layer). TASK-052. |
| BUG-017 | 2026-05-24 | Здания «висели в воздухе» вне дорог (могли быть на depth 2-5 от ближайшей road-клетки) | `UnitPlanner.nextPosition` переписан: только depth=1 + явная проверка `footprintAdjacentToRoad` (хотя бы 1 клетка footprint'а 4-cardinal смежна с road). Anchor-точки пробуются с 4 сторон здания → больше валидных позиций при том же требовании adjacency. + reactive `extendDistrictPlan` сразу резервирует новые road-клетки в `allCells` (раньше только в plan), чинит первопричину — иначе adjacency-фильтр всегда видел только старые road-клетки. + `engine.onRoadCellsAdded` callback и `GameScene.drawAddedRoadCells` для визуала. |
| BUG-018 | 2026-05-24 | Здания пересекали дорогу (footprint накладывался на road-клетки) | `footprintBlocked` уже проверял (после TASK-041), но добавлены проверки в новом nextPosition: каждый кандидат проходит и через blocked, и через adjacent. После исправления BUG-017 пути legacyRingPosition больше не используются при наличии дорог. |
| BUG-010 | 2026-05-24 | Первый юнит проекта не road — здания «в чистом поле». В templated mode (F-25) фиксируется через жёсткое правило `kind = .road` при `project.taskCount == 1 && project.templateName != nil` + slot-placement выбирает road-слот шаблона как первую позицию. В legacy mode (templateName == nil) bug остаётся открытым (известное ограничение, не блокер: legacy-проекты получают road первым через `RoadNetwork.consumeNextPlanCell`). TASK-048c (commit 72ffcbd), см. `CityEngineTemplateAssignmentTests.testFirstUnitIsRoadInTemplate`. |

## Закрытые (старая таблица)

| ID | Закрыт | Описание | Решение (коммит/PR) |
|----|--------|----------|---------------------|
| BUG-011 | 2026-05-24 | Кварталы «лучами разлетаются» — здания строились 8-ю прямыми лучами до 60 тайлов от origin | `UnitPlanner.nextPosition` теперь собирает все свободные кандидаты (depth 1–5, не выходит в legacyRing при наличии дорог), возвращает `GridPoint?`. `CityEngine.applyTaskCompleted` реактивно вызывает `extendDistrictPlan` до 5 раз, пока не найдётся позиция. `footprintBlocked` проверяет и дороги, и занятые здания. `RoadNetwork.extendDistrictPlan` ограничен 20 петлями. TASK-041 |
| BUG-012 | 2026-05-24 | Road-юниты квартала рендерились как 3D-кубоид вместо плоского тайла | Early return в `makeStageNode` для `kind == .road` → `makeRoadCellNode()`. TASK-042 |
| BUG-013 | 2026-05-24 | `weightByDiff` переполнял город — каждый коммит давал 1-5 юнитов без глобального контроля | Добавлен `commitWeightMultiplier` (default 0.1) в AppSettings; слайдер в GitWatcherSection (0.05–2.0). Multiplier применяется в `GitWatcher.performScan` на результат `weightFromLines`. Аналогично `taskWeightMultiplier` (default 1.0) для NotesWatcher и TasksJsonlWatcher. TASK-043 |
| BUG-014 | 2026-05-24 | Все здания занимали 1×1 клетку — `UnitKind.size` не был выставлен по каталогу | Переписан каталог: только {shack, house, well, road, dugout, hut, obelisk, raw} остаются 1×1; остальные ~40 юнитов получили корректные GridSize (1×2..4×4) согласно таблице TASK-044. Спрайты `placeholderSpecs` и z-sort в `drawUnit` обновлены. TASK-044 |
| BUG-015 | 2026-05-24 | Высокая CPU-нагрузка при import-фазе — ~1500 subprocess `git diff --shortstat` за один импорт | Заменён на один `git log --numstat --pretty=tformat:COMMIT\t%H\t%ct\t%s` + локальный парсер в `performScan`. `computeWeight` и `parseDiffShortstat` удалены, заменены pure-функцией `weightFromLines`. TASK-045 |
| BUG-016 | 2026-05-24 | Эволюция «лачуга → дом → усадьба» работает по тупому счётчику kind-of-same, не учитывая остальной квартал | Реализован `EvolutionGraph` (TASK-046): 10 правил с cross-unit `EvolutionRequirement`. `applyEvolutionsIfReady` итерирует по `EvolutionGraph.rules`, проверяет все требования, эволюционирует строго при их выполнении. Cascade limit 5. `evolvesTo`/`evolutionThreshold` deprecated. TASK-046 |

## Приоритеты

- **P0** — ломает ключевой flow / монетизацию
- **P1** — UX-дефект, заметный пользователю
- **P2** — косметика, edge case
