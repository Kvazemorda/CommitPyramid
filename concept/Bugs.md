# CityDeveloper — Баги

_Баги — отдельный поток от diff. Они не считаются при подсчёте сходимости diff
(фича может быть ✅ и при этом иметь баг)._

## Активные

| ID | Приоритет | Статус | Связан с F-NN | Описание | Воспроизведение | Ожидаемое |
|----|-----------|--------|---------------|----------|-----------------|-----------|
| BUG-001 | P1 | 🔴 Открыт | F-11 | Две информационные плашки при клике на дом | Кликнуть по любому юниту → видны и SpriteKit-попап (`InspectorPanel`, world-coords), и SwiftUI overlay-карточка (`InspectorOverlayCard`, screen-coords) одновременно | Одна плашка — современная SwiftUI overlay. SpriteKit-попап убрать (legacy от MVP). |
| BUG-002 | P2 | 🔴 Открыт | F-11 | Окно журнала открывается в случайной позиции | Кликнуть `list.bullet` → floating-окно появляется по умолчанию macOS WindowController (центр или последняя позиция) | Открывается прижатым к правой стороне экрана, по вертикальному центру |
| BUG-003 | P1 | 🔴 Открыт | F-14 | Окно настроек слишком маленькое — кнопка «Add path/repo» обрезана и не нажимается | Открыть Settings (⌘,) → секции NotesWatcher и GitWatcher не помещаются полностью | minWidth/minHeight Settings окна увеличить так, чтобы все кнопки секций (включая «Add path», «Add repo») были видимы без скролла. |
| BUG-004 | P2 | 🔴 Открыт | F-18 | Невозможно подключить Apple Notes как источник — только folder с .md файлами | Settings → Notes watcher → можно выбрать только папку. Свои заметки из встроенного Notes.app подключить нельзя | Добавить адаптер для Apple Notes (через AppleScript / ScriptingBridge / NoteStore.sqlite). Backlog → новая F-NN, не быстрый фикс. |
| BUG-005 | P1 | 🔴 Открыт | F-14, F-20 | Нет UI для сброса состояния и replay из git c указанной даты | Сейчас single-shot CatchUpScheduler сканит репозитории с `lastCheckTs`. Если нужно «затереть всё и начать с 2025-01-01» — приходится вручную удалять `events.jsonl`/`state.json`/`catchup-state.json` | Settings → Reset section: кнопка «Reset city», DatePicker «Replay since», подтверждение «Сотрётся текущий город. Все коммиты с указанной даты будут импортированы заново.». |
| BUG-006 | P1 | ⚠️ Частично — нужен retest после BUG-005 | F-15 | Цвета биомов сломаны — на карте только зелёный и белый | После TASK-027/028 на запуске карта должна иметь 7 биомов разных цветов (sea-teal, river-blue, sand, forest-green, mountain-grey, rock-brown, etc) из `Palette`. По факту: только зелёный (grass) и белый (вероятно snow/mountain). | Палитра обновлена 2026-05-23 (commit 81b829a). Но юзер видит старую карту — `worldmap.json` закэширован. Retest после реализации BUG-005 (Reset). |
| BUG-007 | P1 | 🔴 Открыт | F-14 | Settings секции уезжают за пределы окна после добавления нескольких репозиториев — нет скролла | После BUG-003 окно стало 720×600. Но как только добавляешь 2+ git репо в GitWatcherSection — список разрастается, другие секции (NotesWatcher, hotkey, tasks.jsonl, catch-up) уезжают вниз и недоступны | Обернуть VStack контент в ScrollView, чтобы любое количество источников скроллилось внутри окна, остальные секции оставались доступны. |
| BUG-008 | P1 | 🔴 Открыт | F-15 | BiomeClassifier даёт перекошенное распределение — 80%+ карты один биом (зелёный grass), остатки видны только белым (snow или mountain). Должно быть ~7 биомов с заметными участками каждого | Reset карты → визуально только 2-3 цвета на 256×256 поле, не считая мелких полосок реки | Тюнинг порогов в `BiomeClassifier.classify(world:)`: квантильные пороги пересмотреть, чтобы каждый из 7 биомов занимал ≥5% карты. Логирование (BUG-006 commit) поможет диагностике. |
| BUG-009 | P1 | 🔴 Открыт | F-06, F-15 | Кварталы строятся прямо на воде (реки и моря). Здания должны строиться только на земле | `DistrictPlanner.allocateNextOrigin` использует спираль от центра без учёта `BiomeMapReader`. Если центральная клетка спирали попала в воду — квартал на воде | DistrictPlanner должен пропускать water-биомы (`.sea`, `.river`). Только специальные юниты (порт/верфь) могут быть на воде или near-water — это валидация на уровне UnitPlanner через TerrainAffinity (TASK-033 уже задал water-weight 0.05 для не-портовых). DistrictPlanner — продвигать спираль дальше, пока origin не land. |
| BUG-010 | P1 | 🔴 Открыт | F-07, F-08 | Первый юнит проекта не road — здания появляются «в чистом поле» без связи. Должен быть road как фундамент квартала, потом остальные юниты вокруг | `UnitPlanner.nextUnitKind(forTaskIndex: 0, ...)` сейчас по `categoryPattern` выбирает residential / infrastructure / production / social по позиции в pattern. road — один из `infrastructure` юнитов и появляется случайно | Hardcode rule: при `taskIndex == 0` (или `unitIds.isEmpty`) всегда возвращать `.road`. Потом обычная категориальная rotation. Это даст «дорога — первый юнит» инвариант для всех новых проектов. Старые проекты не меняются (replay deterministic). |

## Закрытые

| ID | Закрыт | Описание | Решение (коммит/PR) |
|----|--------|----------|---------------------|

## Приоритеты

- **P0** — ломает ключевой flow / монетизацию
- **P1** — UX-дефект, заметный пользователю
- **P2** — косметика, edge case
